url <- 'https://api.sklik.cz/sandbox/cipisek/RPC2'
session <- xml.rpc(url,
'client.login', .args = list(username = "ppcyachtcharter@seznam.cz", password = "oode6foo"))$session
library(XMLRPC)
url <- 'https://api.sklik.cz/sandbox/cipisek/RPC2'
session <- xml.rpc(url,
'client.login', .args = list(username = "ppcyachtcharter@seznam.cz", password = "oode6foo"))$session
xml.rpc(url, 'campaigns.list', .args = list(session = session))
xml.rpc(url, 'campaigns.list', .args = list( user = list(session = session)))
View(xml.rpc())
View(xml.rpc)
xml.rpc(url, 'campaigns.list', .args = list( user = list(session = session), campaignIds = c(1008600)))
xml.rpc(url, 'campaigns.list', .args = list( user = list(session = session)), campaignIds = c(1008600))
url <- 'https://api.sklik.cz/cipisek/RPC2'
session <- xml.rpc(url,
'client.login', .args = list(username = "ppcyachtcharter@seznam.cz", password = "oode6foo"))$session
xml.rpc(url, 'campaigns.list', .args = list( user = list(session = session)), campaignIds = c(1008600))
cmp <- xml.rpc(url, 'campaigns.list', .args = list( user = list(session = session)), campaignIds = c(1008600))
head(cmp)
cmp$campaigns$value
xml.rpc(url, 'campaigns.get', .args = list( user = list(session = session)), campaignIds = c(1008600))
xml.rpc(url, 'campaigns.list'. args = list(user = list(session = session)))
xml.rpc(url, 'campaigns.list', .args = list(user = list(session = session)))
campaigns <- xml.rpc(url, 'campaigns.list', .args = list(user = list(session = session)))
campaigns
campaigns$campaigns$value$id
campaigns$campaigns
campaigns$campaigns
head(ampaigns$campaigns)
head(campaigns$campaigns)
campaigns$campaigns$value
campaigns$campaigns$value$id
campaign.stats <- xml.rpc(url, 'campaigns.stats', .args = list( user = list(session = session)), campaignIds = c(1008600))
campaign.stats
campaign.stats <- xml.rpc(url, 'campaigns.stats', .args = list( user = list(session = session)), c(1008600))
campaign.stats
campaign.stats <- xml.rpc(url, 'campaigns.stats', .args = list( user = list(session = session)))
campaign.stats
campaign.stats <- xml.rpc(url, 'campaigns.stats',
.args = list(
user = list(session = session),
campaignIds = c(1),
params = list(dateFrom = ("2017-01-20"),
dateFrom = ("2017-01-22"),
granularity = "daily"
)
))
campaign.stats
campaign.stats <- xml.rpc(url, 'campaigns.stats',
.args = list(
user = list(session = session),
campaignIds = c(1),
params = list(dateFrom = ("2017-01-20"),
dateTo = ("2017-01-22"),
granularity = "daily"
)
))
campaign.stats
campaign.stats <- xml.rpc(url, 'campaigns.stats',
.args = list(
user = list(session = session),
campaignIds = c(1),
params = list(dateFrom = "2017-01-20",
dateTo = "2017-01-22",
granularity = "daily"
)
))
campaign.stats
user = list(session = session),
campaignIds = c(1),
params = list(dateFrom = as.Date("2017-01-20"),
dateTo = as.Date("2017-01-22")",
campaign.stats <- xml.rpc(url, 'campaigns.stats',
.args = list(
user = list(session = session),
campaignIds = c(1),
params = list(dateFrom = as.Date("2017-01-20"),
dateTo = as.Date("2017-01-22"),
granularity = "daily"
)
))
campaign.stats
campaign.stats <- xml.rpc(url, 'campaigns.stats',
.args = list(
user = list(session = session),
campaignIds = c(1),
params = list(dateFrom = as.Date("2017-01-20"),
dateTo = as.Date("2017-01-22"),
granularity = "daily"
)
))
campaign.stats
campaign.stats <- xml.rpc(url, 'campaigns.stats',
.args = list(
user = list(session = session),
campaignIds = c(1,2),
params = list(dateFrom = as.Date("2017-01-20"),
dateTo = as.Date("2017-01-22"),
granularity = "daily"
)
))
campaign.stats
campaign.stats <- xml.rpc(url, 'campaigns.stats',
.args = list(
user = list(session = session),
campaignIds = c(int(1),int(2)),
params = list(dateFrom = as.Date("2017-01-20"),
dateTo = as.Date("2017-01-22"),
granularity = "daily"
)
))
campaign.stats
campaign.stats <- xml.rpc(url, 'campaigns.stats',
.args = list(
user = list(session = session),
campaignIds = c(),
params = list(dateFrom = as.Date("2017-01-20"),
dateTo = as.Date("2017-01-22"),
granularity = "daily"
)
))
campaign.stats
campaign.stats <- xml.rpc(url, 'campaigns.stats',
.args = list(
user = list(session = session),
campaignIds = c(1001),
params = list(dateFrom = as.Date("2017-01-20"),
dateTo = as.Date("2017-01-22"),
granularity = "daily"
)
))
campaign.stats
campaign.stats <- xml.rpc(url, 'campaigns.stats',
.args = list(
user = list(session = session),
campaignIds = c(as.integer(1001)),
params = list(dateFrom = as.Date("2017-01-20"),
dateTo = as.Date("2017-01-22"),
granularity = "daily"
)
))
campaign.stats
campaign.stats <- xml.rpc(url, 'campaigns.stats',
.args = list(
user = list(session = session),
campaignIds = c(as.integer(1001), as.integer(1021)),
params = list(dateFrom = as.Date("2017-01-20"),
dateTo = as.Date("2017-01-22"),
granularity = "daily"
)
))
campaign.stats
campaign.stats <- xml.rpc(url, 'campaigns.stats',
.args = list(
user = list(session = session),
campaignIds = c(as.integer(1008601), as.integer(1008603)),
params = list(dateFrom = as.Date("2017-01-20"),
dateTo = as.Date("2017-01-22"),
granularity = "daily"
)
))
campaign.stats
campaign.stats$report$value
campaign.stats$report$value
res <- campaign.stats$report$value
head(res)
res
typeof(res)
as.data.frame(res)
